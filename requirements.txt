# --- Core data handling ---
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet caching for embeddings/candidates

# --- Sequence & ontology parsing ---
biopython>=1.83        # FASTA parsing
obonet>=1.0.0           # go-basic.obo -> networkx graph
networkx>=3.2            # graph ops on the OBO structure (used by obonet)

# --- Protein language models / embeddings ---
torch>=2.2
transformers>=4.40      # ESM2 via HF; also covers ESM3 checkpoints if released as HF models
fair-esm>=2.0.0          # official ESM2/ESM3 utilities, alt. to transformers loading path

# --- Similarity search ---
faiss-cpu>=1.8.0         # k-NN over cached embeddings (swap for faiss-gpu if available)

# --- Sequence alignment (candidate generation) ---
# Diamond / MMseqs2 are external binaries, not pip-installable.
# Install via conda/apt/binary release and call through subprocess wrappers in
# src/goprot/candidates/sequence_similarity.py — not listed here.

# --- Semantic reranking ---
sentence-transformers>=2.7.0   # cross-encoder / bi-encoder scoring of protein text vs GO defs
scikit-learn>=1.4                # blend/ensemble weighting, threshold search utilities

# --- Knowledge graph ---
neo4j>=5.19               # Neo4j Python driver for GO DAG load + queries

# --- LLM orchestration (text synthesis, reranking, conflict agent) ---
anthropic>=0.30.0          # Claude API client
ollama>=0.2.0                # local MedGemma/other model serving, matches existing DrugSense stack

# --- Config & CLI ---
pyyaml>=6.0
tqdm>=4.66
python-dotenv>=1.0.1

# --- Testing ---
pytest>=8.0
pytest-cov>=5.0